# Exact upstream shelf boxes; the scenario's 5 fallback discs decompose the
# two shelves (0-2 big, 3-4 small), so each index gets its shelf's exact AABB
# (duplicates give the identical SDF). Index 5 (robot base) stays a disc.
SAMPLING_C3_OBS_BOXES="0.62,0.0,0.13,0.08;0.62,0.0,0.13,0.08;0.62,0.0,0.13,0.08;0.24,0.0,0.05,0.08;0.24,0.0,0.05,0.08"
