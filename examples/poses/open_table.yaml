# open_table: push the T across an empty table, 180-degree flip.
#
# No obstacles, so clearance is unbounded everywhere and the only binding
# constraint is the arm's reach. See poses/README.md for the conventions.#
# Re-laid out 2026-08-25 with the scene family: the corridor now runs
# y = +0.4 -> -0.4 with every obstacle inside |y| <= 0.3, so both ends of
# the task start clear of the field. Each variant keeps its offset from the
# nominal pose in CORRIDOR coordinates (along-track, cross-track); yaw
# jitter is unchanged. Clearances below are re-measured.

starts:
  "1": [0.3810, 0.4000, 0.0000] # the scene's original start, 40.0 cm clear
  "2": [0.3660, 0.4310, 0.0579] # 41.3 cm
  "3": [0.3910, 0.4370, -0.4082] # 41.8 cm
  "4": [0.3610, 0.3570, 0.3282] # 37.8 cm
  "5": [0.4060, 0.3830, 0.2806] # 42.7 cm

goals:
  "1": [0.3810, -0.4000, 3.1416] # the scene's original goal, 40.0 cm clear
  "2": [0.3970, -0.4310, 3.2400] # 42.8 cm
  "3": [0.4040, -0.3930, 2.7924] # 43.3 cm
  "4": [0.3860, -0.4240, 2.7704] # 44.3 cm
  "5": [0.3590, -0.4240, 2.7338] # 42.6 cm
